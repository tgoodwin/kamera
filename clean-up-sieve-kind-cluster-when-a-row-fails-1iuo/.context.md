During PR 86 clean retry, cass/stale-state-1 failed during controller deployment and run-sieve-baselines.sh exited with the kind cluster still present. This contradicts the guide's teardown description and blocks the next wrapper invocation via its safety precheck. Add reliable failure cleanup (while preserving logs/results) or clearly report and perform cleanup at wrapper level; verify success and failure paths.