Current sosp-ae replay APPLY replaces the Application and drops spec, confounding KRO-2. Port the schema-backed SSA write engine from commit 636fbb3 (based on newer main), register the KRO Application schema, then rerun the locked crash schedule and require missing children without spec loss.