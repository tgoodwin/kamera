Investigate singleton/shared-state adapters in examples/karpenter (switchingClient, nameGeneratingClient, sync.Once cluster/provisioner, singleton ticker enqueue). Determine which are required to emulate real behavior and which induce branch/process artifacts. Implement safer seams or process-isolation requirements with explicit harness capability metadata.