Provisioner batching currently uses wall-clock timers (clock.RealClock plus Batcher.Wait timers), while singleton enqueue uses simclock. Update harness wiring so provisioner batch timing is deterministic under exploration depth/time and add regression coverage.