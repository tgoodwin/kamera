Follow-up from SPRINT-0001 / docs/talk-bug-summaries/F6-stale-ready-true.md: create a focused trace or real-cluster reproduction where the XR is Ready=True before entering the fatal/error path, then verify Ready stays stale while Synced=False. Likely needs a composed resource type that publishes Ready, such as a Pod or downstream XR, rather than the ConfigMap-only R-13 setup.