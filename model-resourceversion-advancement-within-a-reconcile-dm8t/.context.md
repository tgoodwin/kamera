pkg/tracecheck/manager.go intentionally checks every write in one reconcile against the pre-reconcile RV baseline. A real successful API write advances RV immediately; a second Update using the old RV should conflict, while a client using the returned object should carry the new RV. Also add coverage that Update without metadata.resourceVersion is rejected while ordinary merge Patch remains allowed.