Implement the first complete milestone from GitHub issue #82: registered single-version schemas, exact upstream managed-fields merge/ownership semantics, main and status APPLY, CREATE/UPDATE ownership, generic metadata validation, deterministic initial ownership, tests, and documented exclusions. PR should leave dynamic CRDs, multi-version conversion, admission, CEL, and complete built-in validation for follow-up work.