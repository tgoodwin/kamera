apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: widget-composition
spec:
  compositeTypeRef:
    apiVersion: example.org/v1
    kind: XWidget
  mode: Pipeline
  pipeline:
    - step: pipeline
      functionRef:
        name: kamera-stub
