PR 86 head 49f38796 clean retry: RabbitMQ intermediate-state-1 passes with reproduced=True and the expected PVC 15Gi/10Gi mismatch. Full matrix fails on ZooKeeper because bundled CRD uses apiextensions.k8s.io/v1beta1 and Role/RoleBinding use rbac.authorization.k8s.io/v1beta1. Cassandra fails because controller-manifest.yaml uses apiextensions.k8s.io/v1beta1 CRD and admissionregistration.k8s.io/v1beta1 ValidatingWebhookConfiguration. Kubernetes 1.24 rejects these APIs, so all 4 ZooKeeper and 3 Cassandra rows are blocked before their workloads run. Preserve API semantics while porting manifests, then rerun one row per family and the complete matrix.