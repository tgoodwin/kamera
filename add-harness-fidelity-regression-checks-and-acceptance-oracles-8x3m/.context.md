Define and implement fidelity-oriented regression checks per harness (e.g., expected reconcile/effect invariants, no-op update semantics, trigger ordering invariants, function pipeline expectations). Add automated checks to prevent re-introduction of known semantic drifts.