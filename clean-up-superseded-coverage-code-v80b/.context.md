Clean up superseded static-coverage artifacts after v2 closed-loop flow is implemented and validated.

Expected removals/deprecations include:
- HotspotInstance + TranslateHotspots path,
- legacy coverage.Input fabrication model,
- old harness integration artifacts that front-load scenario construction before execution-guided branching.

Also reassess cmd/generate scope so it aligns with graph-contract + profile output responsibilities.
