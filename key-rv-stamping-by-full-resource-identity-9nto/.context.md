pkg/tracecheck/reconciler.go builds rvByName using only namespace/name, then applies it across every kind in the cache frame. Different kinds sharing namespace/name can receive the wrong metadata.resourceVersion. Key by canonical group/kind plus namespace/name and add a regression test.