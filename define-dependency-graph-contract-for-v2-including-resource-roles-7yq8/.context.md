Define a concrete graph JSON contract that the execution-guided pipeline can consume directly, without ad-hoc heuristics. Scope: (1) canonical edge semantics (including how primary trigger relationships are represented), (2) explicit resource role metadata for input generation (e.g., user_facing vs internal/supporting), (3) canonical controller/resource IDs and GVK normalization rules, and (4) backward-compatibility/validation behavior when fields are missing. Deliverable: updated docs/design/dependency-analysis.md contract section + validator expectations in code (or a documented validation plan) so downstream code can rely on this contract.