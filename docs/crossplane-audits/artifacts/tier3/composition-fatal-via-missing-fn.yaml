apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: widget-composition-fatal
spec:
  compositeTypeRef:
    apiVersion: example.org/v1
    kind: XWidget
  mode: Pipeline
  pipeline:
    - step: invoke-missing-function
      functionRef:
        name: function-does-not-exist
      input:
        apiVersion: pt.fn.crossplane.io/v1beta1
        kind: Resources
        resources: []
